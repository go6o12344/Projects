class Ball{
public:
	double x;
	double y;
	const int r;
	Ball(int x, int y, int r)r{r}{
		this->x = x;
		this->y = y;
	}
	void shoot(double pow, double x, double y){
		if(pow < 2 or pow > 5) return;
		
	}
};